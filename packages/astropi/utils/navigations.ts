/**
 * This module contains functions for generating navigations based on the user's configuration.
 * Either header, sidebar, or footer navigations.
 */

/**
 * For a given collection, get all the entries and their categories, for the sidebar navigation.
 * @param collection - The collection to get the entries from
 */
export async function getSidebarNavigation(collection: string) {
  return []
}
